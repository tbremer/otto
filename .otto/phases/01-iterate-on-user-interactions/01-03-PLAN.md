---
phase: 01
plan: 03
wave: 2
depends_on: ["01", "02"]
files_modified: [commands/otto-exec.md]
autonomous: true
---

<objective>
Add a pre-execution preview that shows what will happen before any changes are made, with options to proceed, preview details, or abort.

**Purpose**: Users want to understand what a plan will do before committing. Currently otto-exec shows tasks but doesn't highlight the actual changes (files modified, commands run).
**Output**: Pre-execution preview shows files to be modified, commands to be run, and estimated time, with user confirmation.
</objective>

<context>
@.otto/PROJECT.md
@.otto/CODEBASE.md
@commands/otto-exec.md
@templates/PLAN.md
</context>

<unknowns>
<!-- None — plan format already includes files_modified and verify commands -->
</unknowns>

<tasks>

<task type="auto">
  <name>Create detailed execution preview</name>
  <files>commands/otto-exec.md</files>
  <action>
Expand Step 6 (Present Execution Plan) to show more detail:

Replace current format with:

```
### 6. Present Execution Plan

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 OTTO ► PREVIEW: Plan {phase}-{plan}
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

{objective text}

Tasks ({N} total, ~{estimated_time} min):
  1. {task 1 name}
  2. {task 2 name}
  3. {task 3 name}

Files to be modified:
  • {file 1} — {action: create/modify/delete}
  • {file 2} — {action}

Verification commands:
  • {verify command from task 1}
  • {verify command from task 2}
  • {final verification items}
```

Use `question` tool:
- header: "Ready to execute?"
- options:
  - "Yes, proceed" — start execution
  - "Show task details" — expand each task's <action> section
  - "Dry run" — show what would happen without making changes (future enhancement placeholder)
  - "Cancel" — abort execution

**Estimated time calculation:** Count tasks × 20 minutes (average task duration).

**File action detection:** 
- If file doesn't exist → "create"
- If file exists → "modify"  
- If action mentions "delete" or "remove" → "delete"

Use `glob` to check file existence for each item in `files_modified`.
```

This gives users confidence about what will change.
  </action>
  <verify>Read commands/otto-exec.md Step 6 and confirm preview shows files with actions and estimated time</verify>
  <done>Execution preview shows files (with create/modify), verification commands, and estimated time</done>
</task>

<task type="auto">
  <name>Add "Show task details" expansion</name>
  <files>commands/otto-exec.md</files>
  <action>
Add Step 6a for when user selects "Show task details":

```
### 6a. Expand Task Details (if requested)

If user selects "Show task details", display each task's full action:

```
━━━ Task 1: {name} ━━━
Files: {files}
Action:
  {action text, indented}
Verify: {verify command}
Done when: {done criteria}

━━━ Task 2: {name} ━━━
...
```

After showing details, return to the confirmation prompt:
- "Yes, proceed"
- "Cancel"

(Remove "Show task details" since we just showed them)
```

This lets users inspect plans without opening the file directly.
  </action>
  <verify>Read commands/otto-exec.md and confirm Step 6a exists for task detail expansion</verify>
  <done>Task detail expansion implemented when user requests it</done>
</task>

</tasks>

<verification>
- [ ] Step 6 shows files with create/modify/delete actions
- [ ] Step 6 shows estimated execution time
- [ ] Step 6 offers "Show task details" option
- [ ] Step 6a expands task details when requested
- [ ] All prompts use question tool
</verification>
