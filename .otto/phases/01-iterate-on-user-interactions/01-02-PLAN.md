---
phase: 01
plan: 02
wave: 1
depends_on: []
files_modified: [commands/otto-exec.md, commands/otto-plan.md, commands/otto-research.md]
autonomous: true
---

<objective>
Improve error handling and recovery flows to provide clearer guidance when things go wrong.

**Purpose**: Current error states show what failed but don't always offer clear recovery paths. Users should never be stuck wondering "what do I do now?"
**Output**: All error states include actionable recovery options with specific commands to run.
</objective>

<context>
@.otto/PROJECT.md
@.otto/CODEBASE.md
@commands/otto-exec.md
@commands/otto-plan.md
@commands/otto-research.md
</context>

<unknowns>
<!-- None — recovery patterns clear from codebase analysis -->
</unknowns>

<tasks>

<task type="auto">
  <name>Enhance otto-exec failure recovery</name>
  <files>commands/otto-exec.md</files>
  <action>
Update Step 7f (task failure handling) to provide richer recovery options:

Current format shows "Fix and retry / Skip / Abort" — enhance to:

```
✗ Task {N}: {name}
  Error: {what went wrong}

Recovery options:
  • Fix and retry — I'll attempt to diagnose and fix the issue
  • Manual fix — pause here so you can fix it, then tell me to continue
  • Skip task — mark as skipped, continue to next task
  • Abort plan — stop execution, keep partial progress
  
What would you like to do?
```

Add guidance for common failure types after the options:
- If verification failed: "Tip: Check the <verify> command output above"
- If file not found: "Tip: Ensure the file path in <files> is correct"
- If command failed: "Tip: The bash command may need different arguments"

Use the `question` tool with these as options (not conversational text).
  </action>
  <verify>Read commands/otto-exec.md Step 7f and confirm 4 recovery options plus tips are present</verify>
  <done>Task failure shows 4 clear recovery options with contextual tips</done>
</task>

<task type="auto">
  <name>Add missing plan validation in otto-exec</name>
  <files>commands/otto-exec.md</files>
  <action>
Add a new Step 3.5 (between Parse Plan and Check Dependencies) to validate plan structure:

```
### 3.5 Validate Plan Structure

Before executing, verify the plan has required sections:

Check the PLAN.md contains:
- `<objective>` section (required)
- `<tasks>` section with at least one `<task>` (required)
- `<verification>` section (required)

**If sections missing:**
```
⚠ Plan {phase}-{plan} is incomplete:
  Missing: {list of missing sections}

This plan may have been created manually or corrupted.

Options:
  • Review plan — open the file for inspection
  • Re-plan — run /otto-plan {phase} to regenerate
  • Proceed anyway — attempt execution with incomplete plan
```

Use `question` tool to present options.
```

This catches malformed plans early rather than failing mid-execution.
  </action>
  <verify>Read commands/otto-exec.md and confirm Step 3.5 validates plan structure</verify>
  <done>Plan structure validation added before execution begins</done>
</task>

<task type="auto">
  <name>Improve otto-plan phase not found error</name>
  <files>commands/otto-plan.md</files>
  <action>
Update Step 3 (Validate Phase Exists) error handling:

Current behavior says "list available phases and ask user which one to plan" — make this more explicit:

```
### 3. Validate Phase Exists

...

**If not found:**
```
⚠ Phase {N} not found in PROJECT.md

Available phases:
  • Phase 1: {name} — {status}
  • Phase 2: {name} — {status}

Options:
  • Plan a different phase — I'll ask which one
  • Add Phase {N} — I'll help you define it in PROJECT.md
  • Cancel — stop here
```

Use `question` tool with these options. If "Add Phase", guide user through defining:
- Phase name
- Phase description  
- Expected outcome
Then update PROJECT.md and continue to planning.
```

This turns a dead-end into a productive path forward.
  </action>
  <verify>Read commands/otto-plan.md Step 3 and confirm error handling offers 3 options including "Add Phase"</verify>
  <done>Phase not found error offers path to define missing phase</done>
</task>

</tasks>

<verification>
- [ ] otto-exec.md Step 7f has 4 recovery options with tips
- [ ] otto-exec.md has new Step 3.5 for plan validation
- [ ] otto-plan.md Step 3 error offers "Add Phase" option
- [ ] All error states use question tool (not conversational)
</verification>
